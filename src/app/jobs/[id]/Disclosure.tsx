/**
 * UI-1 — promoted to `src/components/ui/Disclosure.tsx`. Re-exported here, unchanged, so both
 * existing import sites (this file's own `./Disclosure` import in `MatchCard.tsx`, and
 * `@/app/jobs/[id]/Disclosure` in `ApplicationDetail.tsx`) keep resolving without an edit.
 */
export { Disclosure } from "@/components/ui/Disclosure";
